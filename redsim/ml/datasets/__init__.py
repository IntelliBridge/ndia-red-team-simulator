"""Dataset helpers for the adversarial-ML vertical.

``url_features`` is the lexical extractor for the malicious-URLs tabular task
(spec section 11.3.3). Fetching and caching of dataset bytes lives in
``redsim.ml.assets.datasets`` and runs only inside ``redsim ml build-assets``.
"""
