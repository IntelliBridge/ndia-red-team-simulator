"""Hardening recommendations (spec section 16): a deterministic rule layer
(``rules.interpret`` / ``rules.recommend``) and an optional LLM writer
(``narrative.add_narrative``) that rephrases rule output through Pythia and
may add no claim, number or recommendation of its own."""
