# Feature Specification: Reports and Comparison

## Reconciliation with the product spec (2026-09-08)

Under decision D10 (product owner, 2026-09-08) this feature is the feature-level layer beneath the product spec at [docs/superpowers/specs/2026-09-08-adversarial-ml-redteam-spec.md](../../docs/superpowers/specs/2026-09-08-adversarial-ml-redteam-spec.md), which supersedes both earlier spec sources; where this file and the product spec differ, the product spec wins. F007 maps onto two things aegis already has and one thing it does not. **Reports** are the Markdown / JSON / HTML renderers in `aegis/report.py` (`generate_markdown_report`, `generate_json_report`, `generate_html_report` built from the XSS-safe `html_escape` / `render_inline_markdown` / `_md_to_html_min` helpers that the lean design S2 also chose to reuse), orchestrated by `aegis/services/reports.py` (`render_reports`), re-emitted on demand by the Celery task `aegis.report_render` in `aegis/workers/tasks/report.py`, stored at `runs/{run_id}/report.{md,json,html}` in the blob store (`aegis/storage/blobs.py`, S3/MinIO via `aegis/storage/s3.py`) with a filesystem fallback, and served by `GET /v1/runs/{run_id}/report.{ext}` in `aegis/api/v1/reports.py` behind `ensure_run_access` with the strict `default-src 'none'` CSP for HTML and nosniff + `Content-Disposition` downloads for JSON and Markdown (`aegis/api/security_headers.py`); the run page `web/src/app/runs/[id]/page.tsx` already renders the three download links (`ReportLinks`, `reportUrl` in `web/src/lib/api.ts`). For the ML vertical the renderer is extended to emit the `RunRecord` content of `aegis/ml/schema.py` — configuration and `Provenance` (enough to rerun, including `nondeterminism`), `Measurement` rows by family with `n` and `n_correct`, `Observation` rows, `Interpretation(kind="inferred")`, `CandidateRecommendation(status="candidate", validation="not evaluated")` and `limitations` (always including `STANDING_LIMITATIONS`) — plus the Model Robustness Index scorecard with its five subscores, the per-family accuracy table with denominators and the ε curve (D9(ii)); `report` is the last entry of `STAGES`, so the campaign renders its own report when it finishes. John Sasser's "hardening report" (S1 §9, returned by `POST /v1/findings/{id}/harden`) is the candidate-recommendation section of that report, with the optional narrative written through Pythia from metrics and a SHAP text summary only and labelled `narrative_source="llm"` (D5). **Comparison** is new work: the ΔMRI of two compatible runs (S1 §8.6 under the D9 constraints), which the verify-after-harden loop (`verify.replay`, `aegis/workers/tasks/verify.py`, D4(a)) produces for the hardened re-run and which a read-only comparison projection exposes for any two runs whose campaign settings are identical (D9(i)). **Export policy and redaction** (F008's `PolicyProjection`) do not exist in aegis — `aegis/audit/redact.py` redacts audit-event detail only — and D006 remains OPEN, so they are Phase B. Every report render and every verify appends to the hash-chained audit log (`aegis/audit/chain.py`, Postgres-backed with JSONL offline; S1 §4 puts "each report" on the chain), and the chain's WORM export to S3 Object Lock (`aegis/storage/worm.py`, `aegis/workers/tasks/worm_export.py`) is F008's, landing at M7. Milestones: reports land with **M3** (rule layer + LLM writer produce the report), the scorecard with the MRI step of the D8 demo-critical order, and ΔMRI comparison with **M6** (verify loop); in the D8 order that is image path → MRI scorecard → verify-after-harden, before tabular (M4), ONNX upload and Fargate (M7). Authorization is aegis's: Keycloak OIDC / NextAuth session, `ensure_run_access` (any active `ProjectMembership`) for reads, `policy.check` role ranks (`scanner` < `remediator` < `approver` < `admin`, `aegis/api/policy.py`) for mutations, Postgres RLS by `org_id`; Owner ↔ `admin`, Reviewer ↔ `approver`, Analyst ↔ `remediator`, Viewer ↔ any active membership (`scanner`; no read-only role in Phase A). Nothing in the ML vertical is implemented today: the repository holds the restored aegis platform (the report paths above exist and render pentest-era `AegisFinding` reports) and contract-only scaffolding under `aegis/ml/`; this section records intent, not delivered behavior, and no fixture (`tests/ml/fakes.py` `TinyTarget`, CIFAR-10) is ever presented as a result.

**Decision on the ReportSnapshot (D10 asked feature files to decide cheap-versus-Phase-B).** A versioned `ReportSnapshot` table with archive/restore, and `ExportRequest` rows, are **Phase B**: John's Phase A adds no new core tables (S1 §4) and D8 states Phase A already exceeds the 1–2 day build. Phase A gets the immutability that matters for evidence by construction rather than by a snapshot row: the campaign configuration is written once on the `Run` at admission (F003), `Measurement` / `Observation` / `Provenance` content is written once by the worker into `Finding.schema_blob` and content-addressed `Artifact` rows (`sha256`, unique per `run_id` + `kind` + `sha256`), and the MRI record is written once per campaign, so a re-render can never change a number. What a re-render *can* change is status: `Finding.status` (Phase A dismiss-with-reason → `false_positive`, per F006's reconciliation), `validation_state` after a verify, and `reviewer_notes`. The Phase A report therefore states its `generated_at`, the `Run.id`, the sha256 of the campaign configuration, and the `validation_state` of every finding it prints, and the `report.render` audit event records when it was rendered and by whom. The rendered files at `runs/{run_id}/report.{ext}` are overwritten on re-render; if the product spec requires retained prior renders, they become `Artifact` rows of a report kind, not a new table.

**Decision register.** D001 (image classification on open, unclassified aerial / military-vehicle imagery plus a tabular classifier; CIFAR-10 is the CI fixture only) and D005 (per-family metrics with denominators, benign noise control, ε sweep, SHAP per John's table, MRI per D9, severity thresholds per S1 §8.5) are **RESOLVED** in [specs/_shared/decisions.md](../_shared/decisions.md) as of 2026-09-08. D006 (export redaction, licensing, retention, artifact policy) and D007 (accountable owner and reviewers) remain **OPEN**; the "Unresolved decisions" section below predates the register update and is left as written.

### Requirement status after consolidation

Status values: **unchanged** (meaning holds; only the implementing component changes), **amended** (meaning changes under D1–D13 as noted), **superseded** (replaced by a product-spec rule), **deferred to Phase B**.

| Requirement | Status after consolidation | Note |
| --- | --- | --- |
| US1 — Create a versioned report snapshot | amended | Phase A has no user-created snapshot: the campaign renders its report as its last stage and `report.render` re-emits it on demand. "Every selected version remains fixed" holds for measurements, observations, provenance, limitations and denominators (write-once inputs, see the ReportSnapshot decision above); it does not hold for `Finding.status` / `validation_state`, which the report prints as of `generated_at`. A stale or unreadable source (missing `Artifact` in the blob store, a `Job` still `running`) yields an explicit partial label or a render failure, never a report that claims completeness. Named, versioned, archivable snapshots are Phase B. |
| US2 — Export a policy-permitted report | amended | Formats are Markdown, JSON and HTML (the aegis renderers; D1, D10), not PDF — see FR-005. Export is the same `GET /v1/runs/{id}/report.{ext}` a member uses to read the report, gated by `ensure_run_access`; the access matrix's "Viewer: No" on export is not enforceable in Phase A because reading and downloading are one route and `aegis/api/policy.py` has no report action. Redaction and the F008 policy decision are Phase B (D006 OPEN). |
| US3 — Compare compatible runs | amended | Compatibility is D9(i): same model (`Provenance.model_sha256` / `Target.id`), same modality, same attack set and attack params, same ε grid, same reference budget, same sample dataset, split, `n_samples` and `seed`, same MRI weight vector, same `include_control`. The one permitted changed variable is the applied preprocessing defense recorded by `verify.replay` (D4(a)); library versions, `device`, `hostname`, `started_at` and `nondeterminism` are listed as changed or unchanged but do not block. A run with no `Provenance` or no MRI record makes compatibility unknown and blocks the comparison with that reason. The comparison shows per-family deltas with both denominators and ΔMRI with its five per-dimension deltas (D9(iv)); an absent family or ε cell renders as unavailable, never as zero change. |
| FR-001 | amended | Reads ↔ `ensure_run_access` (any active membership; RLS filters cross-tenant rows). Snapshot creation and archive are Phase B. The only Phase A mutation is re-render (`report.render`), which needs a new `Action` member in `aegis/api/policy.py` — the enum has `scan.start`, `verify.replay`, `run.cancel` and others but no report action today; the product spec's access-matrix section fixes its rank. |
| FR-002 | amended | The "exact selected versions" are concrete aegis identifiers: `Run.id` plus the sha256 of the campaign configuration JSON (F003); `Finding.id` with its `schema_blob` and `validation_state`; `Measurement.id` / `Observation.id`; `Artifact.id` + `sha256` for every image, SHAP map and curve; `Provenance` (versions, `model_sha256`, dataset and split, `nondeterminism`); `limitations`; coverage = `n` per family per ε and `RunRecord.stages_done`; scope = `project_id` / `org_id`. "Review versions" and "policy version" are Phase B (F006 revisions, D006). A `schema_version` field in the JSON report is new work — `generate_json_report` emits `run_id`, `generated_at`, `target`, `summary` and `findings` today and `AegisFinding` blobs carry no version. The MRI record (S1 §8.1 inputs, five subscores, aggregate, grade, weights, ε grid, reference budget, attack set) is included verbatim; where it is persisted per Run is fixed by the product spec's section on `RunRecord` / MRI field placement, and F007 reads it from there. |
| FR-003 | amended | Cross-project selection is impossible (RLS plus `ensure_run_access` return 404 for a run outside the caller's memberships). "Unsupported source" = a `Target` with `status="not_implemented"` or a `Finding.schema_blob` that fails `aegis/ml/schema.py` validation, shown as an explicit state. "Partial persistence failure" maps to `Job.status=failed` for the render job (`aegis/workers/job_state.py`; the worker's `task_context` rolls the body back), with the previous rendered files left in place and the failure visible as an HTTP 404 "report not yet rendered" or the prior `generated_at`. Multi-source snapshot composition is Phase B. |
| FR-004 | amended | `partial` is derived, not stored: a Run whose Jobs ended in mixed terminal states, whose `stages_done` lacks `report`, or whose ε grid has cells with no `Measurement`. William's `completed` ↔ aegis `succeeded`; `timed_out` ↔ `failed` with the reason in `Job.error`; `cancel_requested` is the `run.cancel` service action, not a stored state. Under D9(i) the MRI is defined over the declared attack set × ε grid, so a partial campaign prints its completed per-family rows with `n` and the `partial` label and no MRI; the product spec's scoring section is authoritative if it states this differently. The label appears in the run page, the HTML report header, the Markdown header and `report.json`. |
| FR-005 | superseded | PDF is replaced by CSP-hardened HTML; the three formats are rendered from one canonical projection (`generate_markdown_report` → `_md_to_html_min` for HTML; `generate_json_report` for JSON, which S2 requires to equal the `RunRecord` dump so the run can be rerun from `config` + `provenance`). The Markdown report carries the six ordered sections S2's `test_report.py` asserts (one per `RunRecord` group: configuration and provenance, measurements, observations, interpretation, candidate recommendations, limitations). Every format discloses `generated_at`, `schema_version` (new), limitations, the `partial` label, the dataset licence from the asset manifest, and — until D006 resolves — the statement that no export redaction policy was applied. Numbers quoted in demo material (S1 §15's "about 38", "about 71", "+33") are labelled illustrative (D9(v)). |
| FR-006 | deferred to Phase B | The F008 export/redaction policy decision, licence restrictions and field omission wait on D006. Phase A enforcement is the membership gate: a denied actor receives 403/404 from `ensure_run_access` and no bytes; the rendered file exists in the blob store for members but is not retrievable by a non-member. `aegis/audit/redact.py` (`redact_audit_detail`) redacts credentials in audit-event detail only and is not an export redaction engine. |
| FR-007 | amended | Compatibility rules are fixed by D9(i), not by a pending D005 (RESOLVED): the identical-settings list under US3. Variables are reported in three groups — changed (only the applied defense is admissible), unchanged (settings), disclosed environment differences (`Provenance` versions, `device`, `hostname`, `nondeterminism`) — and "unknown" is reserved for a run lacking `Provenance` or an MRI record, which blocks. F006's reconciliation already states that a retest with different settings is recorded as incompatible and carries no ΔMRI. |
| FR-008 | amended | Per-family definitions, both runs' denominators, exclusions, coverage and missing values are retained exactly as written. The clause "MUST NOT be combined into a universal score" is narrowed by D9: ΔMRI and its five per-dimension deltas are shown for compatible runs of one model × one modality × identical settings, never aggregated across modalities or domains, never compared across differing settings, and never without the per-family delta table with `n` and both ε curves (D9(ii)). Recorded as a divergence from the brief's "avoid a universal score" principle, justified as a per-campaign summary that never mixes domains and always travels with its denominators (D9(vi)). |
| FR-009 | unchanged | Enforced by the Literal types of `aegis/ml/schema.py` (`Interpretation.kind="inferred"`, `CandidateRecommendation.status="candidate"` / `validation="not evaluated"`, `Observation.metric_kind="heuristic"`) rendered as separate report sections and separate UI panels. Reviewer judgments are Phase B (F006); Phase A prints `reviewer_notes`, `status` and `validation_state` as of `generated_at`. Retest outcomes are the stored `validation_state` (`poc_passed` / `poc_failed` / `inconclusive`) and the measured ΔMRI, described as the measured effect of the named defense on this model at these settings (D9(iv)) — the only sanctioned form of "gain"; S1 §9's "expected robustness gain" is not printed. Grade-band text is attack-scoped and states that no grade is a readiness or certification statement (D9(iii)). |
| FR-010 | deferred to Phase B | Immutable `ReportSnapshot` rows with Owner archive/restore are Phase B. Phase A: aegis exposes no report edit, archive or delete route; rendered files are overwritten only by an audited `report.render`; retention deletion defers to F008 and D006 (OPEN); the audit chain and its WORM export are append-only (`tests/test_audit_append_only.py`, `tests/test_worm_export.py`). |
| FR-011 | amended | Concrete aegis signals: 400 for an `ext` outside `md|json|html`; 403/404 from `ensure_run_access`; 404 "report not yet rendered" while the campaign or render job is not `succeeded`; an empty `/v1/runs` list; a comparison response whose body lists the blocking reasons (settings differ, `Provenance` or MRI record missing) and carries no delta; unavailable families rendered as unavailable. Export timeout and format-generation failure surface as `Job.status=failed` with `Job.error`. Redacted-source states are Phase B. No protected content leaks because responses to non-members carry no run data. |

Also affected: the Scope bullet "PDF and JSON exports after F008 redaction/export policy evaluation" becomes "Markdown, JSON and HTML reports; redaction/export policy Phase B"; the Exclusion "Universal assurance scores" is narrowed to "any score aggregated across modalities or domains, or compared across campaigns with different settings" (D9); the Exclusion "Tactical, weapon, live-target … reporting" stands with the D3 bounds recorded — reports may show open, unclassified, publicly licensed aerial / military-vehicle imagery, the tool evaluates and hardens a classifier's robustness and never trains, optimizes or deploys targeting or weapons models, and no mission system is connected; the Exclusion "Automatically treating a changed metric as proof a recommendation caused improvement" stands, with ΔMRI presented as the measured effect of one applied defense under otherwise identical settings, not as validation of a recommendation in general. Key entities: `ReportSnapshot` → Phase A rendered `report.{md,json,html}` plus its `report.render` audit event, Phase B table; `ExportRequest` → Phase A the audited render, Phase B table; `RunComparison` → a read-only projection over two Runs' MRI records and `Measurement` rows (no table); `PolicyProjection` → F008, Phase B. Edge cases "runs use the same profile ID but different immutable profile versions" and "a metric definition changes while retaining a similar display label" are handled by comparing the configuration sha256 and `Measurement.id` / `attack_id` / params rather than labels. Success criteria: SC-001 applies in Phase A to the write-once inputs and the `partial` label; SC-002 reduces in Phase A to the membership gate (100% of non-member requests receive 403/404 with zero run content) and regains its redaction clause in Phase B; SC-003's "zero sampled outputs contain a universal score" is read as "zero outputs contain an MRI aggregated across modalities or domains, compared across differing settings, or shown without its subscores, the per-family table with `n` and the ε curve".

### plan.md and tasks.md items that point at Replit monorepo locations

Listed only — `plan.md` and `tasks.md` are left as William Yiu authored them and no task box is checked. "exists" means the file is in the restored aegis platform today; "new" means it does not exist yet.

| Item | Location named there | Correct aegis location |
| --- | --- | --- |
| plan.md Approach step 1 | "existing pnpm monorepo, shared backend, API contract, and future assurance frontend" | FastAPI in `aegis/api/`, Celery in `aegis/workers/`, Next.js app `web/` (`@aegis/web`), shared components `packages/design-system/` (all exist). |
| plan.md Approach steps 4–5; tasks.md T013 wording | "PDF and JSON artifact"; "PDF/JSON canonical mappings" | Markdown / JSON / HTML from `aegis/report.py` and `aegis/services/reports.py` (exist); there is no PDF renderer and none is planned for Phase A. T013's path `specs/007-reports-comparison/contracts/export-formats.md` is a valid spec-tree location; only its format list changes. |
| plan.md Approach step 8; "Foundational contracts" | "accepted minimal F008 event envelope" | `aegis/audit/chain.py` `AuditWriter.append(action=…, actor=…, target=…, detail=…)` (exists; `PostgresAuditWriter`, `JsonlAuditWriter`); dotted action names in the existing style, e.g. `report.render`. |
| plan.md "Proposed API edits"; tasks.md T005 | `lib/api-spec/openapi.yaml`, regenerated `lib/api-client-react/`, `lib/api-zod/` | No hand-maintained OpenAPI file and no generated client exist: FastAPI serves `/openapi.json` from the routers in `aegis/api/v1/*.py`; the typed web client is hand-written in `web/src/lib/api.ts` (`reportUrl` exists). Nothing is regenerated. |
| plan.md "Proposed schema"; tasks.md T006 | `lib/db/src/schema/report-snapshots.ts` | Phase A adds no table. Phase B `ReportSnapshot` / `ExportRequest` go in `aegis/db/models.py` plus an Alembic revision under `aegis/db/migrations/versions/`. |
| plan.md "Proposed routes"; tasks.md T008 | `artifacts/api-server/src/routes/assurance/reports.ts` | `aegis/api/v1/reports.py` (exists; `GET /v1/runs/{run_id}/report.{ext}`), mounted in `aegis/api/app.py`; the comparison read is a new route beside it (a `compare` endpoint in `aegis/api/v1/runs.py` or a new `aegis/api/v1/compare.py`), gated by `ensure_run_access` on both runs. |
| plan.md "Proposed services" (`report-snapshots.ts`); tasks.md T007 | `artifacts/api-server/src/services/assurance/report-snapshots.ts` | `aegis/services/reports.py` (`render_reports`, exists) and the ML renderer extending `aegis/report.py` (or an `aegis/ml/report.py` reusing its `html_escape` / `render_inline_markdown` / `_md_to_html_min` helpers); re-emission via `aegis/workers/tasks/report.py` (`aegis.report_render`, exists). Phase B snapshot service: a new module under `aegis/services/`. |
| plan.md "Proposed services" (`report-export.ts`); tasks.md T012, T014 | `artifacts/api-server/src/services/assurance/report-export.ts` | Serving and headers: `aegis/api/v1/reports.py` + `aegis/api/security_headers.py` (exist). Export redaction/policy is Phase B and has no aegis module yet; `aegis/audit/redact.py` is audit-detail redaction only, not this. |
| plan.md "Proposed services" (`run-comparison.ts`); tasks.md T018 | `artifacts/api-server/src/services/assurance/run-comparison.ts` | New pure comparison module under `aegis/ml/` (for example `aegis/ml/compare.py`) operating on two campaign/score records and `Measurement` lists from `aegis/ml/schema.py`; the ΔMRI written by verify lives in `aegis/workers/tasks/verify.py` (exists, extended at M6). |
| plan.md "Proposed frontend feature"; tasks.md T009, T010, T015 | `artifacts/ai-assurance/src/features/reports/` (`ReportComposer.tsx`, `ReportSnapshotView.tsx`, `ReportExport.tsx`) | `web/src/app/runs/[id]/page.tsx` (exists; `ReportLinks` already renders the HTML / JSON / Markdown links, and the page gains the scorecard, table and curve). The report view itself is the server-rendered HTML from `GET /v1/runs/{id}/report.html`. There is no Phase A composer; a Phase B composer goes under `web/src/app/reports/`. Shared pieces in `packages/design-system/src/components/`. |
| tasks.md T019, T020 | `artifacts/ai-assurance/src/features/reports/RunComparison.tsx`, `ComparisonMetrics.tsx` | New `web/src/app/runs/[id]/compare/page.tsx` (or a compare panel on the run page) with `packages/design-system/src/components/run-comparison.tsx` and `comparison-metrics.tsx` (new; kebab-case per the design-system convention). |
| plan.md "Proposed backend checks"; tasks.md T011, T016, T021 | `artifacts/api-server/src/tests/assurance/reports-comparison.test.ts`, `report-snapshots.test.ts`, `report-export.test.ts`, `run-comparison.test.ts` | `tests/test_report_xss.py` (exists; HTML escaping and CSP) and route/access checks in flat `tests/` with the `sqlite_session_factory` conftest harness (pattern: `tests/test_project_access_read.py`); ML report and comparison logic in `tests/ml/` with the `ml` marker and `TinyTarget` from `tests/ml/fakes.py` (S2's `test_report.py` cases: six sections in order, `<script>` in a class name escaped, JSON equals the `RunRecord` dump). |
| plan.md "Proposed frontend checks"; tasks.md T022 | `artifacts/ai-assurance/src/tests/reports-comparison.test.tsx` | Co-located Vitest files: `web/src/app/runs/[id]/page.test.tsx` (exists; already asserts the three report links) and a new `web/src/app/runs/[id]/compare/page.test.tsx`. |

Paths that are already correct but whose meaning shifts: tasks.md T001 (`specs/007-reports-comparison/plan.md`, decision routing) is record-keeping for D006/D007 only, because D001 and D005 are RESOLVED; T002, T003, T004, T013, T017 and T023 point at `specs/007-reports-comparison/*` review artifacts and remain valid, with T003's interface contract mirroring the FastAPI routers rather than an OpenAPI source of truth and T017's compatibility inventory fixed by D9(i) rather than a pending D005. Plan gates G1 (D001) and G2 (D005) are closed by D11; G3 (D006) and G5 (D007) remain open; G4 (F004 and F006 contracts) is satisfied by `aegis/ml/schema.py` plus the F006 reconciliation's state mapping.


**Feature:** F007 — Reports and Comparison  
**Status:** Draft / not approved for implementation  
**Owner:** Unassigned (suggested role: assurance reporting lead)  
**Approval required:** Product owner, engineering reviewer, security/data reviewer
## What and why

Reports and comparison create immutable, versioned snapshots of selected run evidence and reviewed findings, then provide policy-permitted PDF and JSON exports. Compatible run comparisons identify exactly which controlled variables changed, preserve coverage and partial-run labels, and avoid a universal score across unrelated measures.
## Scope

- Authorized creation and reading of versioned ReportSnapshot records.
- Snapshot selection of F004 run/evidence versions and F006 finding/review revisions.
- PDF and JSON exports after F008 redaction/export policy evaluation.
- Comparison of compatible runs with explicit changed and unchanged variables.
- Metric-family denominators, coverage, limitations, provenance, and partial-run labels.
- Explicit denied, incompatible, empty, redacted, stale-selection, and export-failure states.

## Exclusions

- Editing immutable source runs, evidence, findings, reviews, or snapshots.
- Universal assurance scores, certification, operational-readiness claims, or causal claims.
- Export that bypasses project membership, object authorization, license, or redaction policy.
- Tactical, weapon, live-target, sensitive-data, or attack-recipe reporting.
- Automatically treating a changed metric as proof a recommendation caused improvement.

## User stories

### US1 — Create a versioned report snapshot (P1)

As an authorized analyst or reviewer, I want a fixed report snapshot so the report remains traceable when later findings or labels change.

**Independent test:** Create and reopen a snapshot from one completed or partial run and verify every selected version, limitation, denominator, and status remains fixed.

- **Given** authorized compatible source records, **When** a user creates a snapshot, **Then** exact run, evidence, finding, and review versions plus scope and policy version are retained.
- **Given** the selected run is partial, **When** the snapshot is viewed, **Then** every relevant summary and export is prominently labeled partial.
- **Given** a selection becomes stale or unreadable, **When** creation is attempted, **Then** creation fails explicitly and no incomplete snapshot is presented as successful.

### US2 — Export a policy-permitted report (P1)

As an authorized exporter, I want PDF or JSON output with required redaction so I can share only approved report content.

**Independent test:** Export one permitted snapshot in both formats and attempt denied/redacted fields, verifying policy is enforced server-side.

- **Given** an Owner, Analyst, or Reviewer is permitted by F001 and F008 policy, **When** PDF or JSON is requested, **Then** the export records format, snapshot version, redaction policy, and limitations.
- **Given** a Viewer or policy-denied actor requests export, **When** the request is evaluated, **Then** it fails without generating or disclosing the file.
- **Given** fields require redaction, **When** export succeeds, **Then** omission/redaction markers are truthful and protected values are absent.

### US3 — Compare compatible runs (P2)

As a reviewer, I want two compatible runs compared by metric family with changed variables listed so I can interpret differences honestly.

**Independent test:** Compare a compatible pair and an incompatible pair; only the former yields a comparison and it lists all known changed variables.

- **Given** two runs satisfy approved compatibility rules, **When** compared, **Then** changed and unchanged model, dataset, profile, evaluator, and environment variables are explicit.
- **Given** runs are incompatible or compatibility is unknown, **When** comparison is requested, **Then** it is blocked with reasons and no misleading delta is calculated.
- **Given** a metric is absent from either run, **When** the comparison renders, **Then** it shows unavailable coverage rather than zero change.

## Functional requirements

- **FR-001:** Report reads, snapshot creation, archive actions, and exports MUST require active F001 membership plus server-side project, object, and role authorization.
- **FR-002:** Authorized users MUST be able to create an immutable ReportSnapshot containing exact selected run, evidence, finding, review, scope, provenance, limitations, coverage, policy, and schema versions.
- **FR-003:** Snapshot creation MUST reject stale, inaccessible, cross-project, or unsupported source selections and MUST not claim success after partial persistence failure.
- **FR-004:** A partial source run MUST be labeled `partial` in snapshot views and every export location where its results appear.
- **FR-005:** PDF and JSON exports MUST represent the same snapshot semantics and disclose format, generation time, schema version, limitations, and redaction policy version.
- **FR-006:** Export authorization, license restrictions, field omission, and redaction MUST be enforced server-side using the approved F008 policy; denied exports MUST not produce retrievable artifacts.
- **FR-007:** Comparisons MUST proceed only for runs satisfying approved compatibility rules and MUST list known changed, unchanged, and unknown variables.
- **FR-008:** Comparison metrics MUST retain per-family definitions, denominators, exclusions, coverage, and missing values and MUST NOT be combined into a universal score.
- **FR-009:** Reports and comparisons MUST distinguish observations, reviewer judgments, candidate recommendations, and retest outcomes without attributing causality.
- **FR-010:** ReportSnapshots MUST be immutable after creation; an authorized Owner MAY archive a snapshot, restore it if policy permits, but ordinary edit or delete MUST be unavailable and retention deletion MUST defer to F008.
- **FR-011:** Empty report lists, no comparable runs, unavailable/redacted sources, unknown compatibility, export timeout, and format-generation failure MUST be explicit and must not leak protected content.

## Key entities

- **ReportSnapshot:** Immutable selected versions, scope, coverage, provenance, limitations, partial label, redaction policy version, and archive status.
- **ExportRequest:** Actor, snapshot version, PDF or JSON format, policy decision, outcome, and non-sensitive artifact reference.
- **RunComparison:** Two source runs, compatibility result, changed/unchanged/unknown variables, metric-family deltas, and limitations.
- **PolicyProjection:** F008-owned export/redaction decision consumed by F007.

## Edge cases

- One selected finding revision is superseded after snapshot creation.
- A report contains both complete and partial source runs.
- PDF succeeds while a separately requested JSON export fails.
- Export permission is revoked between request and retrieval.
- Runs use the same profile ID but different immutable profile versions.
- A metric definition changes while retaining a similar display label.
- An archived snapshot is still referenced by an authorized historical audit event.

## Success criteria

- **SC-001:** Every sampled report/export resolves to exact immutable source versions and displays all applicable limitations and partial-run labels.
- **SC-002:** In focused checks, 100% of denied or redaction-required export cases enforce policy server-side with zero protected field disclosure.
- **SC-003:** Every accepted comparison lists known changed variables and retains metric denominators/coverage; zero sampled outputs contain a universal score.

## Unresolved decisions

- **D001:** First benign domain and report vocabulary.
- **D005:** Metric definitions, denominators, and compatibility criteria.
- **D006:** Export redaction, licensing, retention, and artifact policy.
- **D007:** Accountable owner and independent reviewers.