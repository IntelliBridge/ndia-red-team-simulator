kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
name: redsim
nodes:
  - role: control-plane
    extraPortMappings:
      - containerPort: 30080  # api
        hostPort: 8000
      - containerPort: 30030  # web
        hostPort: 3000
      - containerPort: 30090  # keycloak
        hostPort: 8080
